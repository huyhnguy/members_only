<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title>Message Board</title>
</head>
<body>
    <h1>Join the club!</h1>
    <p>Gain access to see the authors and dates of all journal entries</p>
    <p>Hint: the passcode is 4 digit code of the month and day of Christmas! MMDD</p>
    <form action="" method="POST">
        <label for="code">Secret Passcode</label>
        <input type="text" name="code">
        <button type="submit">Submit</button>
    </form>
    <% if (errors) { %> 
        <ul>
            <% errors.forEach((error) => { %>
                <li><%= error.msg %></li>
            <% }) %>
        </ul>
    <% } %>
</body>
</html>